server:
  port: 1399
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  # SSE 流式优化：减小 socket buffer 让每次 flush 立即发送给客户端
  tomcat:
    socket:
      buffer: 1024

spring:
  mvc:
    async:
      # 禁用 Spring MVC 异步请求超时，改由 RelayService 业务层超时管控：
      # - 非流式：MAX_TOTAL_TIMEOUT_MS（600s）总超时兜底
      # - 流式：SseEmitter(0L) 无墙钟超时 + STREAM_IDLE_TIMEOUT_MS（60s 空闲超时）
      # 若不显式禁用，Tomcat 默认异步超时会强制关闭长请求（AsyncRequestTimeoutException），
      # 导致客户端收到不完整 HTTP 响应（hyper::Error(IncompleteMessage)）。
      request-timeout: -1
  application:
    name: my-ai-gateway
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  sql:
    init:
      mode: never
  datasource:
    url: 'jdbc:sqlite:data/gateway.db'
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-init-sql: "PRAGMA journal_mode=WAL;PRAGMA synchronous=NORMAL;PRAGMA cache_size=-64000;PRAGMA temp_store=MEMORY;PRAGMA mmap_size=268435456;PRAGMA foreign_keys=ON;PRAGMA busy_timeout=30000;"

# MyBatis-Plus
mybatis-plus:
  global-config:
    db-config:
      id-type: AUTO
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl

# Logging
logging:
  level:
    com.myai.gateway: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] [%X{traceId}] %-5level %logger{36} - %msg%n"

# App config (管理员账号从数据库读取)
app:
  jwt:
    # JWT 签名密钥：通过环境变量 APP_JWT_SECRET 覆盖，未设置时使用内置默认值（仅适合开发，生产环境必须修改）
    # 启动时若仍为默认值或长度不足会打印 WARN 日志（见 JwtTokenProvider#validateSecret）
    secret: ${APP_JWT_SECRET:my-ai-gateway-jwt-secret-key-2024-change-in-production}
    # Token 过期时间（毫秒），365 天，实现持久登录
    expiration: 31536000000
  # 超时配置说明：RelayService 的超时由 LatencyTracker 自适应计算，
  # 系统配置项 timeout_min_seconds / timeout_max_seconds 在管理后台设置，此处不配置。

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: when_authorized
